@!(data)
School
@{edu = data.educations.values[0]}
@edu.startDate.year
@data.major @edu.degree
@data.minor Minor
@edu.schoolName@for(i=0; i<data.certifications.values.length; i++) {
  @{cert = data.certifications.values[i]}
@cert.startDate.year
@cert.name
@cert.authority.name
}
